import React, { useEffect } from 'react';
export function Dialog({open,onOpenChange,children}){useEffect(()=>{const onKey=e=>{if(e.key==='Escape'&&open) onOpenChange?.(false)};window.addEventListener('keydown',onKey);return()=>window.removeEventListener('keydown',onKey)},[open,onOpenChange]);if(!open) return null;return <div className="fixed inset-0 bg-black/45 z-50 flex items-center justify-center" onClick={(e)=>{if(e.target===e.currentTarget) onOpenChange?.(false)}}>{children}</div>}
export function DialogContent({className="",...p}){return <div className={["bg-white w-full max-w-xl rounded-2xl shadow-xl",className].join(" ")} {...p}/>}
export function DialogHeader({className="",...p}){return <div className={["p-4 border-b border-gray-200",className].join(" ")} {...p}/>}
export function DialogFooter({className="",...p}){return <div className={["p-4 border-t border-gray-200 flex justify-end gap-2",className].join(" ")} {...p}/>}
export function DialogTitle({className="",...p}){return <h2 className={["text-xl font-semibold",className].join(" ")} {...p}/>}
export function DialogDescription({className="",...p}){return <div className={["text-sm text-gray-600",className].join(" ")} {...p}/>}